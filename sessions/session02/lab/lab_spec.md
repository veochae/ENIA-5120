# Lab 02 – ggplot Practice

## Goals

- Build multiple plots from the same dataset
- Apply themes and annotations

## Tasks

1. Reuse `sales_df` from Week 1.
2. Create a scatter plot of `price` vs `quantity`, colored by `category`.
3. Create a bar chart of mean `price` by `category`.
4. Save each plot as `plot_scatter.png` and `plot_bar.png`.
