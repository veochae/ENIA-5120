total_points: 20
criteria:
  - id: diagnostics
    description: "Runs initial EDA checks (missingness, histogram) and interprets findings."
    max_points: 6
  - id: imputation
    description: "Implements deletion, mean imputation, median imputation, and compares outcomes."
    max_points: 8
  - id: grouping_visuals
    description: "Applies mode imputation, builds role_eda summary, and produces the outlier boxplot with interpretation."
    max_points: 6
