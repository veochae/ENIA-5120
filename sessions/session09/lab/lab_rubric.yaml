total_points: 20
criteria:
  - id: open_meteo_pull
    description: "Builds a valid no-auth Open-Meteo request and parses `weather_df` correctly."
    max_points: 8
  - id: nasa_key_pull
    description: "Uses `NASA_API_KEY` (or DEMO fallback), calls APOD, and builds `apod_df` correctly."
    max_points: 8
  - id: interpretation
    description: "Provides clear reflection on auth differences, data cleaning, and reliability risk."
    max_points: 4
