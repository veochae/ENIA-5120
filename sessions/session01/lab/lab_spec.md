# Lab 01 – Intro to R

## Goals

- Load CSV into R
- Inspect rows/columns
- Compute quick summaries

## Tasks

1. Load `data/week1_sales.csv` as `sales_df`.
2. Print `nrow`, `ncol`, and column names.
3. Use `str()` to describe column types.
4. Create `summary_tbl` that groups by `category` and keeps mean `price`.
5. Knit the template and submit to Canvas.
