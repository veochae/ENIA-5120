# Lab 03 – Data Visualization

## Goals
- Practice multiple geoms and aesthetics
- Use facets to compare groups
- Apply themes and labels for clarity

## Tasks
1. Load `~/ENIA-5120/data/laser_league_players.csv`.
2. Create `hist_plot` showing the distribution of `season_score` with bins + median line.
3. Create `box_plot` comparing `tag_accuracy_pct` by `role`.
4. Create `scatter_plot` of `assists` vs `season_score`, colored by `preferred_loadout`, faceted by `home_arena`.
5. Save all ggplot objects for grading.
